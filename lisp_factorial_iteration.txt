

(defun factorial(n)
    (setq a 1)
    (loop for i from 1 to n
        do(setf a (* a i)))
a)

(print "Enter a number:")
(finish-output)
(setq x (read))

(format t "~D! = ~D" x (factorial x))